spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/nono_logic
    username: postgres
    password: 123321
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
